apiVersion: v1
kind: Service
metadata:
    name: "sncakstestgeb-bac1"
    labels:
        app: "sncakstestgeb-bac1"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "sncakstestgeb-bac1"