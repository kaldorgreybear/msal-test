apiVersion: v1
kind: Service
metadata:
    name: "spidermonkey-a47b"
    labels:
        app: "spidermonkey-a47b"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "spidermonkey-a47b"