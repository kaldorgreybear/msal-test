apiVersion: v1
kind: Service
metadata:
    name: "gebakscluster22-75c8"
    labels:
        app: "gebakscluster22-75c8"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "gebakscluster22-75c8"