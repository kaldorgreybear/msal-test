apiVersion: v1
kind: Service
metadata:
    name: "sncakstestgeb-e92f"
    labels:
        app: "sncakstestgeb-e92f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "sncakstestgeb-e92f"